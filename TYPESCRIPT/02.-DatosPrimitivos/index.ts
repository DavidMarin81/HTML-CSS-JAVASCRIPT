//DATOS PRIMITIVOS

//Boolean (true - false)
    let verdadero : boolean = true;

//Number
    let numero : number = 20;
    let decimal : number = 19.34;
    let hexadecimales : number = 0x123acb;
    let octales : number = 0o700;
    let binario : number = 0b11001011;
    /* Los guiones bajos se usan como separadores para facilitar la lectura de numeros muy largos*/
    let numeroLargo : number = 1_203_456;

//String
    let cadenas : string = "Hola mundo";

/* Variables propias de Typescript */

//Any (no se suele utilizar)
    /* (acepta cualquier cosa. Se puede cambiar de tipo en cualquier momento. Se usa cuando se importan archivos .js y nos da algun aviso de error) */
    let cualquiera : any = "df";

//Null
    /* no se le puede asignar nada */ 
    let nada : null = null;

//Undefined
    let indefinido : undefined = undefined;

//void
    /* se usa para definir que una variable no devuelva nada */
   